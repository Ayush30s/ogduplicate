<!DOCTYPE html>
<html lang="en">
<head>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        /* Custom Dropdown Animation */
        .dropdown-enter {
            transform: translateY(-10px);
            opacity: 0;
            transition: all 0.3s ease-out;
        }
        .dropdown-enter-active {
            transform: translateY(0);
            opacity: 1;
        }
        /* Navbar underline effect */
        .nav-item {
            position: relative;
            transition: color 0.3s ease-in-out;
        }
        .nav-item::after {
            content: '';
            position: absolute;
            width: 0;
            height: 2px;
            background: red;
            left: 50%;
            bottom: -2px;
            transition: all 0.3s ease-in-out;
            transform: translateX(-50%);
        }
        .nav-item:hover::after {
            width: 100%;
        }
        /* Smooth accordion transition */
        #mobile-menu {
            transition: max-height 0.5s ease-in-out;
        }
    </style>
</head>
<body class="bg-black text-white font-sans">
    <!-- Nav Bar -->
    <nav class="bg-black border-b-2 border-red-500 z-50 py-2">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="relative flex items-center justify-between h-16">
                <!-- Logo -->
                <div class="flex-shrink-0">
                    <a href="#" class="text-red-500 text-3xl font-extrabold tracking-widest hover:text-white transition-all duration-300 ease-in-out">
                        My<span class="text-white">Gym</span>
                    </a>
                </div>
                <!-- Menu Items (Hidden on Small Devices) -->
                <div class="hidden sm:flex sm:ml-6 items-center space-x-6">
                    <a href="/home" class="nav-item text-red-500 px-3 py-2 rounded-md text-lg font-medium hover:text-white transition-colors duration-300 ease-in-out">Home</a>
                    <a href="/home/about" class="nav-item text-red-500 px-3 py-2 rounded-md text-lg font-medium hover:text-white transition-colors duration-300 ease-in-out">About</a>
                    <a href="#" class="nav-item text-red-500 px-3 py-2 rounded-md text-lg font-medium hover:text-white transition-colors duration-300 ease-in-out">Services</a>
                    
                    <!-- Dropdown Menu on Right Side (Visible on larger screens) -->
                    <div class="relative group hidden sm:block">
                        <button class="text-red-500 rounded-full text-lg font-medium border border-red-600 hover:bg-red-500 hover:text-black focus:outline-none transition-transform duration-300 ease-in-out transform hover:scale-105">
                            <% if (locals.user) { %>
                                <img class="w-16 rounded-full" src="<%= locals.user.profileImage %>" alt="">
                            <% } else { %>
                                <a href="/app/register-user" class="block px-4 py-1 text-red-500 hover:bg-red-500 hover:text-black">SignUp</a>
                            <% } %>
                        </button>
                        <!-- Dropdown Items -->
                        <% if (locals.user) { %>
                            <div class="absolute right-1 mt-2 w-44 text-center bg-black rounded-md shadow-lg opacity-0 invisible group-hover:visible group-hover:opacity-100 transition-all duration-300 ease-out dropdown-enter dropdown-enter-active z-10">
                                <a href="/home/profile/<%= user._id %>" class="block px-4 py-2 text-red-500 hover:bg-red-500 hover:text-black hover:font-bold border border-black">Profile</a>
                                <a href="/register/signout" class="block px-4 py-2 text-red-500 hover:bg-red-500 hover:text-black hover:font-bold">SignOut</a>
                            </div>
                        <% } %>
                    </div>
                </div>

                <!-- Mobile Menu Button -->
                <div class="sm:hidden flex items-center">
                    <button id="mobile-menu-button" class="text-red-500 hover:text-white focus:outline-none">
                        <svg class="h-8 w-8" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7" />
                        </svg>
                    </button>
                </div>
            </div>
        </div>

        <!-- Mobile Menu (Accordion) -->
        <div id="mobile-menu" class="sm:hidden overflow-hidden max-h-0">
            <div class="px-2 pt-2 pb-3 space-y-1">
                <a href="/home" class="text-red-500 block px-3 py-2 rounded-md text-lg font-medium hover:bg-red-500 hover:text-black shadow-2xl shadow-red-600">Home</a>
                <a href="/home/about" class="text-red-500 block px-3 py-2 rounded-md text-lg font-medium hover:bg-red-500 hover:text-black shadow-2xl shadow-red-600">About</a>
                <a href="#" class="text-red-500 block px-3 py-2 rounded-md text-lg font-medium hover:bg-red-500 hover:text-black shadow-2xl shadow-red-600">Services</a>
                
                <!-- Email and Profile links (Visible on mobile screens) -->
                <% if (locals.user) { %>
                    <div class="block px-3 py-2 rounded-md text-lg font-medium text-red-500">
                        <%= locals.user.email %>
                    </div>
                    <a href="/home/profile/<%= user._id %>" class="block px-3 py-2 text-red-500 hover:bg-red-500 hover:text-black shadow-2xl shadow-red-600">Profile</a>
                    <a href="/register/signout" class="block px-3 py-2 text-red-500 hover:bg-red-500 hover:text-black shadow-2xl shadow-red-600">SignOut</a>
                <% } else { %>
                    <a href="/app/register-user" class="block px-3 py-2 text-red-500 hover:bg-red-500 hover:text-black shadow-2xl shadow-red-600">SignUp</a>
                <% } %>
            </div>
        </div>
    </nav>

    <script>
        // JavaScript for toggling the mobile menu with smooth opening/closing
        const mobileMenuButton = document.getElementById('mobile-menu-button');
        const mobileMenu = document.getElementById('mobile-menu');

        mobileMenuButton.addEventListener('click', () => {
            if (mobileMenu.classList.contains('max-h-0')) {
                mobileMenu.classList.remove('max-h-0');
                mobileMenu.classList.add('max-h-screen');
            } else {
                mobileMenu.classList.add('max-h-0');
                mobileMenu.classList.remove('max-h-screen');
            }
        });
    </script>
</body>
</html>
